# test-project Pipeline

This pipeline was generated by `multiagent new`.

## Quick Start

```bash
# Run pipeline
python cli.py --pipeline pipeline/test-project.yaml --output human --save-artifacts

# With preset
python cli.py --pipeline pipeline/test-project.yaml --preset mvp-fast

# Dry-run
python cli.py --pipeline pipeline/test-project.yaml --dry-run --export-graph out/test-project.dot
```

## Stages

1. **requirements** - Define project requirements
2. **code_skeleton** - Generate initial code structure

## Customization

Edit `pipeline/test-project.yaml` to:
- Add more stages
- Adjust score thresholds
- Configure advisors
- Set budget limits

See [docs/INDEX.md](../docs/INDEX.md) for complete documentation.
